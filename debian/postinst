#!/bin/bash
set -e

# Update desktop database
update-desktop-database -q || true

# Note: We no longer create a system-wide config file in /etc
# Users will have their individual configs created in ~/.config/profile_switcher
# when they first run the application
